(()=>{const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));let data=[];const input=document.getElementById('quiz-search');const box=document.getElementById('quiz-search-results');if(input&&box){fetch('/assets/quiz-index.json').then(r=>r.json()).then(x=>data=x).catch(()=>{});input.addEventListener('input',()=>{const q=input.value.trim().toLowerCase();if(q.length<2){box.hidden=true;box.innerHTML='';return;}const hits=data.filter(i=>(i.title+' '+i.copy+' '+i.type).toLowerCase().includes(q)).slice(0,8);box.innerHTML=hits.map(i=>'<a href="'+i.url+'"><span><strong>'+esc(i.title)+'</strong><small> · '+esc(i.copy)+'</small></span><em>'+esc(i.type)+'</em></a>').join('');box.hidden=!hits.length;});}const player=document.getElementById('quiz-player');const start=document.querySelector('[data-start-quiz]');if(!player||!start)return;const src=player.dataset.quizSrc;const qEl=player.querySelector('[data-question]');const optsEl=player.querySelector('[data-options]');const feedback=player.querySelector('[data-feedback]');const next=player.querySelector('[data-next]');const progress=player.querySelector('[data-progress]');const scoreEl=player.querySelector('[data-score]');const meter=player.querySelector('[data-meter]');let quiz=null,order=[],idx=0,score=0,mistakes=[],started=0,answered=false;const shuffle=a=>{const x=a.slice();for(let i=x.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[x[i],x[j]]=[x[j],x[i]];}return x;};async function load(){if(quiz)return quiz;const res=await fetch(src);quiz=await res.json();return quiz;}function current(){return quiz.questions[order[idx]];}function render(){const q=current();answered=false;feedback.hidden=true;feedback.innerHTML='';next.disabled=true;progress.textContent='Question '+(idx+1)+' / '+order.length;scoreEl.textContent='Score '+score;meter.style.width=Math.round((idx/order.length)*100)+'%';qEl.textContent=q.question;optsEl.innerHTML=q.options.map((o,i)=>'<button type="button" data-option="'+i+'"><strong>'+String.fromCharCode(65+i)+'.</strong> '+esc(o)+'</button>').join('');optsEl.querySelectorAll('button').forEach(b=>b.addEventListener('click',()=>answer(Number(b.dataset.option))));}function answer(choice){if(answered)return;answered=true;const q=current();const buttons=[...optsEl.querySelectorAll('button')];buttons.forEach((b,i)=>{b.disabled=true;if(i===q.answer_index)b.classList.add('correct');if(i===choice&&i!==q.answer_index)b.classList.add('wrong');});const ok=choice===q.answer_index;if(ok)score++;else mistakes.push({q,choice});const correct=q.options[q.answer_index];feedback.hidden=false;feedback.innerHTML='<strong>'+(ok?'Correct!':'Incorrect')+'</strong>'+(ok?'':'<p>Correct Answer: '+esc(correct)+'</p>')+(q.explanation?'<p>'+esc(q.explanation)+'</p>':'');scoreEl.textContent='Score '+score;next.disabled=false;}function finish(){const seconds=Math.max(1,Math.round((Date.now()-started)/1000));const accuracy=Math.round((score/order.length)*100);meter.style.width='100%';qEl.textContent='Score';optsEl.innerHTML='<div class="result-card"><div class="result-grid"><div><span>Score</span><strong>'+score+' / '+order.length+'</strong></div><div><span>Time Taken</span><strong>'+Math.floor(seconds/60)+'m '+(seconds%60)+'s</strong></div><div><span>Accuracy</span><strong>'+accuracy+'%</strong></div></div><div class="result-actions"><button type="button" data-review>Review Mistakes</button><button type="button" data-retake>Retake Quiz</button></div><div class="mistake-list" data-mistakes hidden></div></div>';feedback.hidden=true;next.disabled=true;next.textContent='Done';optsEl.querySelector('[data-retake]').addEventListener('click',begin);optsEl.querySelector('[data-review]').addEventListener('click',()=>{const list=optsEl.querySelector('[data-mistakes]');list.hidden=!list.hidden;list.innerHTML=mistakes.length?mistakes.map((m,i)=>'<article><strong>Q'+(i+1)+'. '+esc(m.q.question)+'</strong><p>Your answer: '+esc(m.q.options[m.choice]||'Not answered')+'</p><p>Correct answer: '+esc(m.q.options[m.q.answer_index])+'</p>'+(m.q.explanation?'<p>'+esc(m.q.explanation)+'</p>':'')+'</article>').join(''):'<article>No mistakes in this attempt.</article>';});}function goNext(){if(idx>=order.length-1){finish();return;}idx++;render();}async function begin(){start.disabled=true;start.textContent='Loading quiz...';await load();order=shuffle(quiz.questions.map((_,i)=>i));idx=0;score=0;mistakes=[];started=Date.now();next.textContent='Next';player.hidden=false;start.textContent='Restart Quiz';start.disabled=false;render();player.scrollIntoView({behavior:'smooth',block:'start'});}start.addEventListener('click',begin);next.addEventListener('click',goNext);})();